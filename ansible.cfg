[defaults]
inventory = ./inventories/hosts.yml
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent
forks = 10
timeout = 30

# Automatically prompt for Vault password on every run
ask_vault_pass = True

# Show file diffs when changes are made
diff = True
